var PanelCtrl = function($scope, 
						OrdenesFactory,
						$state, 
						$log,
						$ionicHistory) {
	
	$log.debug("PanelCtrl", $scope.$id);
	var self = this;

};

app.controller('PanelCtrl', PanelCtrl);
